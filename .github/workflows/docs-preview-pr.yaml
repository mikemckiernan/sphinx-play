name: docs-preview-pr

on:
  workflow_run:
    workflows: [build]
    types: [requested]

env:
  WF_ID: ${{ github.event.workflow_run.id }}

jobs:
  remove:
    uses: nvidia-merlin/.github/.github/workflows/docs-preview-pr-common.yaml@docs-chore
